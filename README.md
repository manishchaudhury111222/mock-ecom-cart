# Mock E-Com Cart 🛒

A full-stack shopping cart app built for Vibe Commerce screening.

## Tech Stack
- **Frontend:** React + Vite
- **Backend:** Node.js + Express + File-based mock DB (JSON)
- **DB:** JSON persistence (easily switchable to MongoDB)
- **API Endpoints:** /api/products, /api/cart, /api/checkout

## Run Locally
### Backend & Frontend
```bash
cd backend
npm install
npm start


cd frontend
npm install
npm run dev
